# General Helpers
alias cls="clear"
alias gs="git status"
alias par="php artisan"
alias nace="node ace"
alias explorer='explorer.exe `wslpath -w "$PWD"`'

# Cloud Foundry
alias cf-ssh="cf ssh -t -c \"/tmp/lifecycle/launcher /home/vcap/app bash ''\""